import { StyleSheet } from 'react-native'
import { colors } from '../../../styles'

export default StyleSheet.create({
  container: {
    paddingBottom: 10
  },
  value: {
    fontSize: 50,
    paddingVertical: 6
  }
})

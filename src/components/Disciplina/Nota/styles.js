import { StyleSheet } from 'react-native'
import { colors } from '../../../styles'

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center'
  },
  value: {
    fontSize: 36,
    fontWeight: '300',
    color: colors.mutted
  },
  title: {
    fontSize: 14,
    fontWeight: '500',
    color: colors.mutted
  }
})

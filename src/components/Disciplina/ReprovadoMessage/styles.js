import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingVertical: 26
  },
  message: {
    fontSize: 14,
    fontWeight: '500',
    color: '#333'
  }
})

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    borderColor: '#EEE',
    backgroundColor: '#FFF',
    borderWidth: 1,
    marginBottom: 15,
    borderRadius: 3
  },
  wrapper: {
    padding: 15,
  }
});

export default styles;

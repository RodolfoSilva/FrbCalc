import { StyleSheet } from 'react-native'
import { colors } from '../../../styles'

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center'
  },
  value: {
    fontSize: 38,
    fontWeight: '200',
    color: colors.mutted
  },
  title: {
    fontSize: 14,
    fontWeight: '300',
    color: colors.mutted
  }
})
